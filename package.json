{
  "name": "devops-eval-api",
  "version": "1.0.0",
  "scripts": {
    "start": "node src/server.js",
    "test": "jest --coverage --coverageReporters=lcov --coverageReporters=text",
    "lint": "eslint src/"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.4",
    "eslint": "^8.57.0"
  },
  "jest": {
    "coverageThreshold": {
      "global": { "lines": 70 }
    }
  }
}
